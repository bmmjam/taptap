# TapTap 🐹

**Telegram Mini App для определения эмоционального состояния через тапы**

TapTap — это интерактивное приложение для групповых мероприятий, которое помогает участникам понять своё эмоциональное состояние через простое взаимодействие с интерфейсом. Пользователь тапает по смайлику в течение нескольких секунд, а приложение анализирует характер взаимодействия (интенсивность, скорость, движения) и определяет текущее настроение.

## Возможности

- **Интерактивный тест**: Пользователь тапает по смайлику в течение 6 секунд
- **Анализ эмоций**: Определение одного из 6 эмоциональных состояний:
  - 😤 Стресс
  - 🤩 Энергия
  - 😌 Спокойствие
  - 😟 Тревога
  - 🧐 Фокус
  - 😔 Грусть
- **Групповая статистика**: Просмотр общего настроения группы
- **Визуальная панель**: Графики и диаграммы для отображения результатов группы
- **Telegram интеграция**: Работает как Mini App внутри Telegram

## Структура проекта

```
taptap/
├── bot/                    # Telegram бот (Python)
│   ├── bot.py             # Основной код бота + HTTP API
│   └── requirements.txt   # Зависимости Python
├── webapp/                # Frontend приложение
│   ├── index.html        # Главная страница Mini App
│   ├── app.js            # Логика приложения
│   └── style.css         # Стили
├── presentation.html      # Презентация для мероприятия
├── qr.png                # QR-код для быстрого доступа
└── LAUNCH.md             # Инструкции по запуску
```

## Технологии

### Backend
- **Python 3.14+**
- **aiogram** — асинхронная библиотека для Telegram Bot API
- **aiohttp** — HTTP сервер для API

### Frontend
- Vanilla JavaScript (ES6+)
- Chart.js — визуализация данных
- Telegram Web App API

## Быстрый старт

### Требования

- Python 3.14+
- Telegram Bot Token (получить у [@BotFather](https://t.me/botfather))
- Cloudflared или другой туннель для локальной разработки

### 1. Установка зависимостей

```bash
cd bot
python -m venv .venv
source .venv/bin/activate  # для Linux/Mac
# или .venv\Scripts\activate для Windows
pip install -r requirements.txt
```

### 2. Запуск туннеля

В отдельном терминале:

```bash
/tmp/cloudflared tunnel --url http://localhost:8080
```

Скопируйте URL вида `https://xxx-xxx.trycloudflare.com`

### 3. Запуск бота

```bash
export BOT_TOKEN='ваш_токен_бота'
export WEBAPP_URL='https://bmmjam.github.io/taptap/webapp/'
export API_URL='https://xxx-xxx.trycloudflare.com'  # URL из шага 2

bot/.venv/bin/python bot/bot.py
```

### 4. Использование

1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Нажмите кнопку "😊 Узнать моё состояние"
4. Тапайте по смайлику в течение 6 секунд
5. Получите результат и посмотрите общую статистику группы командой `/group`

## Команды бота

- `/start` — Запустить бота и открыть Mini App
- `/group` — Показать текстовую статистику группы
- `/reset` — Сбросить все результаты (доступно только организаторам)

## API Endpoints

Бот предоставляет HTTP API для интеграции:

- `POST /api/result` — Сохранить результат пользователя
- `GET /api/results` — Получить все результаты группы
- `POST /api/reset` — Сбросить результаты

### Пример запроса

```javascript
POST /api/result
Content-Type: application/json

{
  "user_id": "123456",
  "name": "Иван",
  "emotion": "excited",
  "stats": {
    "taps": 45,
    "avg_speed": 120,
    "intensity": 0.8
  }
}
```

## Разработка

### Локальное тестирование webapp

Для тестирования frontend без Telegram:

```bash
cd webapp
python -m http.server 8000
```

Откройте http://localhost:8000 в браузере.

### Деплой webapp

Frontend размещён на GitHub Pages:
```bash
git subtree push --prefix webapp origin gh-pages
```

## Использование на мероприятиях

1. Перед мероприятием:
   - Запустите туннель и бота
   - Проверьте работоспособность через `/start`
   - Подготовьте QR-код для быстрого доступа

2. Во время мероприятия:
   - Участники сканируют QR или переходят по ссылке
   - Каждый проходит тест (6 секунд)
   - Используйте `/group` для отображения статистики
   - Между раундами используйте `/reset` для очистки результатов

3. Советы:
   - Не закрывайте ноутбук — туннель прервётся
   - Держите терминал с ботом открытым
   - Проверьте стабильность интернет-соединения

## Презентация

В проекте есть готовая HTML-презентация (`presentation.html`) для представления TapTap на мероприятиях. Откройте файл в браузере и используйте стрелки или пробел для навигации.

## Принцип работы

1. **Сбор данных**: Приложение отслеживает тапы, движения пальца, гироскоп устройства
2. **Анализ**: Вычисляются метрики:
   - Интенсивность тапов
   - Скорость взаимодействия
   - Характер движений (хаотичные/плавные)
   - Использование акселерометра
3. **Классификация**: На основе метрик определяется эмоциональное состояние
4. **Визуализация**: Результаты отображаются индивидуально и в групповой статистике

## Лицензия

MIT

## Автор

Разработано для групповых интерактивных мероприятий и исследований эмоционального состояния.

---

**Примечание**: Это экспериментальный проект для образовательных целей. Результаты теста носят развлекательный характер и не являются профессиональной психологической оценкой.
